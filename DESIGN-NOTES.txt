Views:
- Index screen -> last 10 publication works (only title, authors, type, rating)
- Publication work details -> show everything + rating option
- Create publication -> title textbox; authors textbox (split by comma); publication date input; publication genre dropdown; publication type (book/magazine); issue (if magazine)
- Filters page -> filter publication works by author; filter publication works by genre;